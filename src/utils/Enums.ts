
// ROLE ENUMS
export enum RoleType {
    VENDOR = 'vendor',
    ADMIN = 'admin'
}

//USER ACCOUNT STATUS ENUMS
export enum AccountStatus {
    ACTIVE = 'activate',
    DEACTIVE = 'deactivate'
}

//OTP SEND TYPE ENUMS
export enum OtpType {
    FORGET = 'forget',
    VERIFICATION = 'verification'
}